[package]
name = "kernel"
description = "STAR OS microkernel — the privileged core binary"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[[bin]]
name = "kernel"
path = "src/main.rs"

[dependencies]
staros-abi.workspace = true
staros-hal.workspace = true
staros-fdt.workspace = true
staros-framebuffer.workspace = true
staros-mm.workspace = true
staros-ipc.workspace = true
staros-drivers.workspace = true
# The arch backend. When more architectures land this becomes a cfg-selected
# dependency; today aarch64 is the only target.
staros-arch-aarch64.workspace = true
